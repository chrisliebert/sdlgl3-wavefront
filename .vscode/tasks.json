{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-sdlglapp-debug",
            "type": "shell",
            "command": "powershell",
            "args": [
                "-NoProfile",
                "-ExecutionPolicy",
                "Bypass",
                "-Command",
                "$p = Get-Process -Name 'sdlglapp' -ErrorAction SilentlyContinue; if ($p) { $p | Stop-Process -Force }; cmake --build build --config Debug -j 6"
            ],
            "group": "build",
            "problemMatcher": [
                "$msCompile"
            ]
        }
    ]
}